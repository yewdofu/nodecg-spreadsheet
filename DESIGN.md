# Google Spreadsheet連携Bundle 設計書

## 1. 文書の位置付け

- 要件の根拠: [PLAN.md](PLAN.md)
- 状態: **確認待ちの設計ドラフト**。未決事項があるため、実装仕様としては未確定。
- 「確定」はPLAN.mdに明記された内容とユーザーの回答を反映した内容、「現状」は既存コードで確認した内容を指す。
- 「案」は検討用の提案であり、採用済みの仕様ではない。未回答の項目に既定値を設定しない。
- 本書では設計を扱う。実装コード、依存パッケージ、既存のサンプルは変更しない。

## 2. 確定している要件

スピードランイベント向けの、Google Spreadsheetと連携するNodeCG Bundleを開発する。

| 対象 | 確定事項 |
| --- | --- |
| シート種別 | プレイヤー一覧、解説者一覧、スケジュール |
| シートの配置 | 1つのGoogleスプレッドシート内に、上記3種類のタブを置く |
| 実際のシート名 | bundleConfigで定義する |
| プレイヤー一覧・解説者一覧の項目 | `name`, `twitch_id`, `twitter_id`, `youtube_id`, `game_name`, `category` |
| スケジュールの項目 | `start_time`, `game_name`, `release_year`, `category`, `estimate`, `players`, `commentators` |
| スケジュールの複数人指定 | `players`と`commentators`はカンマ区切り |
| マスターデータ | Google Spreadsheetをマスターとして運用する |
| Googleへのアクセス | 読み取り専用。シートへの書き戻しは行わない |
| 認証 | サービスアカウントを使用する |
| Dashboard | データ編集とスケジュール選択を可能にする |
| 編集対象・操作 | プレイヤー一覧・解説者一覧・スケジュールすべての値の変更・行の追加・削除 |
| 開始時刻 | `start_time`は日付を含む、記載者・イベントの現地時刻。シートへのタイムゾーン・UTCオフセットの記入は不要 |
| タイムゾーン | イベントのタイムゾーンをbundleConfigで設定し、シートの日時の解釈に使用する |
| スケジュールの順序 | 開始日時の早い順、空欄は末尾。同一開始日時は`game_name`の50音順。手動の並べ替えは不要 |
| ゲームタイトルの比較 | 読みの別カラムは管理せず、タイトル表記そのものを日本語の文字列比較で並べる |
| 同期時の編集内容 | 手動・自動同期でシートの内容に上書きする |
| 人物の照合 | `players`はプレイヤー一覧の`name`、`commentators`は解説者一覧の`name`と照合する |
| 同名人物の絞り込み | `name`に複数行が一致する場合、`game_name`・`category`も一致する行に絞り込む |
| 人物一覧のゲーム・カテゴリ | 両一覧に`game_name`と`category`が存在する |
| 手動同期 | Dashboardのボタンで明示的に実行する |
| 自動同期 | 指定秒数間隔で実行できる |
| 自動同期の設定 | 有効・無効と間隔の秒数をbundleConfigで設定する |
| 起動時 | 起動直後にも同期する |
| 呼称 | 「スケジュール」に統一する |

これらの項目が物理的な列見出しか、列順が固定か、入力必須かはPLAN.mdに記載されていない。

本書の「シート」は、連携先の1つのGoogleスプレッドシート内にあるタブを指す。別々のスプレッドシートファイルへの接続は対象に含めない。

## 3. 既存プロジェクトの現状

| 場所 | 現状と設計上の関係 |
| --- | --- |
| `package.json` | Bundle名は`nodecg-bundle-template`。NodeCG、TypeScript、React、Viteを使用する構成。Google接続用の依存パッケージは未定義 |
| `configschema.json` | 設定項目は未定義。シート名などの設定を設計する対象 |
| `src/extension/index.ts` | Extensionの入口。サンプル処理を呼び出す |
| `src/extension/example.ts` | `example` Replicantのカウンターを更新するサンプル |
| `schemas/example.json` | サンプルReplicantのJSON Schema |
| `src/types/generated/` | Schemaから生成された型。変更時は生成元Schemaを更新する構成 |
| `src/browser/dashboard/views/Example.tsx` | Dashboardのサンプル表示 |
| `src/browser/graphics/views/Example.tsx` | Graphicsのサンプル表示 |
| `vite.config.mts` | Bundle名とブラウザー・Extensionのビルド対象を設定 |

READMEではDashboardとGraphicsにReactを使用する方針が示されている。Dashboardには編集・同期・スケジュール選択の機能が必要と確定した。Graphicsの要否は未決であり、サンプルの存在を機能要件とはみなさない。

## 4. 構成案

シートを読み取り、NodeCG側で利用・編集する構成とする。Dashboardの編集はシートには書き戻さず、手動・自動同期でシートの内容に上書きする。各処理の分割と公開手段は以下の案とする。

| 構成要素 | 担当する処理の案 | 確定に必要な事項 |
| --- | --- | --- |
| 設定読み込み | bundleConfigを検証し、接続先と各シートの対応を取得 | Q2、Q5 |
| Google接続 | サービスアカウントによる認証と読み取りをExtension側で担当 | Q2の残件 |
| 行データの解析 | シートの列と論理項目の対応付け、値の解釈 | Q5、Q6 |
| データ検証・参照解決 | 項目の検証、`name`による人物とスケジュールの対応付け | Q4の残件、Q6、Q7 |
| 同期制御 | ボタン操作と指定秒数間隔による同期、実行状態の管理 | Q3の残件、Q7、Q9 |
| NodeCGへの公開 | 利用側にデータ、選択中のスケジュール、同期結果を提供 | Q8、Q11 |
| Dashboard | データ編集、手動・自動同期、スケジュール選択を提供 | Q8、Q10、Q11 |

取得、解析、検証、公開を分ける案とし、形式不正や通信失敗を個別に扱えるようにする。具体的なモジュール名・公開API名は契約確定後に決める。

### 4.1 Googleからの取得

連携先は1つのスプレッドシートとし、その中の3つのタブを読み取る。Google Sheets APIの`spreadsheets.values.batchGet`で各タブの範囲をまとめて取得する案とする。使用ライブラリと各タブの取得範囲は未決。[Google公式リファレンス](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets.values/batchGet)

表示済みの値と未整形の値では取得結果が異なるため、`start_time`・`estimate`・`release_year`の入力・公開形式を確認してから`valueRenderOption`を選ぶ。[Google公式ガイド](https://developers.google.com/workspace/sheets/api/guides/values)

認証にはサービスアカウントを使用する。認証情報をExtension側で扱い、公開データや画面用のエラー情報に含めない案とする。資格情報の置き場所と供給方法はQ2の残件。Google APIの権限は読み取り専用の要件に合わせて設計する。

### 4.2 NodeCGへの公開

既存構成に合わせ、共有データにはReplicantを使用する案とする。Replicantは同名のJSON Schemaで値を検証できるため、公開データ形式が確定した後にSchemaを定義し、既存の`generate-schema-types`で型を生成する。[NodeCG公式ドキュメント](https://www.nodecg.dev/fr/docs/replicant-schemas/)

| 案 | 利点・検討点 |
| --- | --- |
| 3シート分を1つのReplicantに格納 | 1回の公開でデータの組を置き換える構成にできる。全体更新時のデータ量を検討する必要がある |
| シート種別ごとにReplicantを分割 | 必要なデータを個別に購読できる。更新途中に新旧データが混ざらないよう、世代の識別などが必要 |

いずれを採用するか、同期状態を別Replicantにするか、永続化するか、他Bundleが参照する名前は未決。APIによる一括取得を、シート編集に対する同一時点のスナップショット保証と同一視しない。

### 4.3 Dashboardの操作

| 操作 | 確定した動作 | 未決事項 |
| --- | --- | --- |
| 編集 | 3種類すべての値の変更・行の追加・削除が可能。シートには書き戻さず、次回同期でシートの内容に上書きする | 保存操作、取得中の編集、他の行から参照される人物の変更・削除 |
| スケジュールの順序 | `start_time`を設定されたイベントのタイムゾーンで解釈し、早い順に並べる。空欄は末尾、同一開始日時はタイトル表記そのものの日本語の文字列比較順。手動の並べ替えは設けない | 日時表記・セル形式、文字列比較の詳細、比較条件が同じ場合の順序 |
| 手動同期 | ボタンで明示的にシートから同期し、編集内容を上書きする | 同期中の再操作、取得中の編集 |
| 自動同期 | bundleConfigの有効・無効と秒数設定に従って同期し、編集内容を上書きする | 設定値・省略時の扱い、許容間隔、設定変更を反映するタイミング |
| スケジュール選択 | 利用するスケジュールを選択できる | 初期選択、解除、選択の保存、再同期時の追跡、公開形式 |

編集・同期・選択要求をExtensionで受け付け、検証後に共有データへ反映する案とする。具体的なメッセージ名や応答形式は、保存操作と競合規則の回答後に定義する。

シートの最新取得結果、Dashboardで編集した運用データ、選択中のスケジュールは、論理上区別して扱う。同期時はシートの内容で運用データを置き換える。編集内容を優先するマージや差分選択は行わない。取得中の編集可否、同期失敗時の扱い、選択状態の維持方法は別途確認する。

スケジュールはシートの取得後とDashboardでの追加・開始日時またはゲームタイトル変更後に、確定した並び順を満たす必要がある。比較規則は第5.4節にまとめる。

### 4.4 bundleConfigに持つ設定

| 設定内容 | 確定事項 | 未決事項 |
| --- | --- | --- |
| 連携先スプレッドシート | 1つのファイル内の3つのタブを読み取る | ファイルIDなどの指定方法・設定キー名 |
| 各シート名 | プレイヤー一覧・解説者一覧・スケジュールの実際のタブ名を指定 | 設定キー名 |
| 自動同期の有効・無効 | bundleConfigで指定 | 設定キー名、省略時の扱い |
| 自動同期の間隔 | bundleConfigで秒数を指定 | 設定キー名、整数のみか、許容範囲、省略時の扱い |
| タイムゾーン | bundleConfigでイベントのタイムゾーンを指定し、シートの現地日時の解釈に使用 | 設定キー名、値の形式 |

起動直後の同期は確定要件として同期制御に含める。起動時の同期完了から間隔を計るかなどの起算点、処理が間隔内に完了しない場合の制御は未決。Dashboardで自動同期設定を編集・保存する機能は設けない。

## 5. データ契約の設計対象

### 5.1 プレイヤー一覧・解説者一覧

両一覧の項目集合は共通だが、人物情報の共通マスター化や両一覧の統合は要件に含まれていない。

| 項目 | 設計時に確認すること |
| --- | --- |
| `name` | 参照の照合項目として使用することは確定。同名、空欄、前後の空白をどう扱うか |
| `twitch_id` | ログイン名・URLなどの入力形式。空欄を許容するか |
| `twitter_id` | `@`付き・なし、URLなどの入力形式。空欄を許容するか |
| `youtube_id` | チャンネルID・ハンドル・URLのどれか。空欄を許容するか |
| `game_name` | カラムの存在と、同名人物の候補の絞り込みに使うことは確定。空欄や表記差の扱いは未決 |
| `category` | `game_name`とともに同名人物の候補の絞り込みに使う。空欄や表記差の扱いは未決 |

同一人物の複数行を自動で統合したり、SNS IDを人物キーとして使ったりする仕様は確定していない。

### 5.2 スケジュール

| 項目 | 設計時に確認すること |
| --- | --- |
| `game_name` | 必須性、照合規則、同一ゲームの複数スケジュールの扱い |
| `release_year` | 数値か表示用文字列か。未定・不明・空欄の扱い |
| `category` | 必須性、同じゲーム・カテゴリのスケジュールが複数ある場合の識別 |
| `estimate` | `H:MM:SS`などの文字列、Sheetsの時間値、分数など、入力形式と公開形式 |
| `players` | カンマ区切りの各要素をプレイヤー一覧の`name`と照合する。空白、重複、空要素、名前内のカンマの扱いは未決 |
| `commentators` | カンマ区切りの各要素を解説者一覧の`name`と照合する。解説なし、空白、重複、空要素、名前内のカンマの扱いは未決 |
| `start_time` | イベントの現地日時。bundleConfigのタイムゾーンで解釈する。空欄は末尾、同一開始日時はゲームタイトルの日本語の文字列比較順。具体的な日時表記・セル形式と内部・公開形式は未決 |

スケジュールの順序は`start_time`順と確定している。選択を追跡するスケジュールIDが必要か、人物参照を文字列・ID・展開済みオブジェクトのどの形式で公開するかは未決。

### 5.3 人物参照の規則

`players`の各要素はプレイヤー一覧の`name`、`commentators`の各要素は解説者一覧の`name`と照合する。両一覧の`game_name`・`category`は入力項目として存在する。

`name`に複数行が一致する場合、対象スケジュールの`game_name`・`category`も一致する行に絞り込む。これにより、同名の行のうちゲームタイトル・カテゴリが異なる行を区別する。

3項目で絞り込んだ結果が0件または複数件の場合の扱いは未決。先頭行を選ぶなどの自動補完は仕様として決めていない。また、名前が1件だけ一致する場合にもゲームタイトル・カテゴリの整合性を必須とするか、空白などの表記差を正規化するかは未決。

### 5.4 スケジュールの比較規則

1. `start_time`に記載された日付と時刻を、bundleConfigで指定されたイベントのタイムゾーンの現地日時として解釈し、早い順に並べる。日付をまたぐスケジュールも入力された日付を含めて比較する。
2. `start_time`が空欄の行は、開始日時がある行より後に配置する。
3. 同一開始日時の行が存在する場合は、`game_name`の50音順で並べる。読みの別カラムは管理せず、タイトル表記そのものを日本語の文字列比較に使用する。

例えば、シートに「2026年9月16日 10時」と記載され、設定が日本のタイムゾーンであれば、日本での同日10時として扱う。これは時刻の意味を示す例であり、シートの表記形式や設定値の形式を指定するものではない。実行サーバーや閲覧者の端末のタイムゾーンでシートの日時を解釈しない。

シート記載者に`Z`や`+09:00`などの記入を求めない。設定タイムゾーンは、表示の変換先だけでなく、シートに記載された現地日時を解釈する基準である。

シートの具体的な日時表記・セル形式と、内部・公開データの日時形式は未決。先のRFC 3339指定については、最新の回答で確定した「シートの記載時刻＝イベントの現地時刻」を入力設計の基準とする。RFC 3339を内部・公開形式に採用するかは別途確定し、シート入力にオフセットを要求する根拠にはしない。

空欄と日時として解釈できない不正値は別のケースとして扱い、不正値を自動で空欄に変換する仕様は定めていない。

タイトルの文字列比較について、英字の大文字・小文字、全角・半角、数字などの扱いと比較ライブラリは未決。タイトル表記の比較に漢字・英字の読みを推定する処理は含めない。空欄の行が複数ある場合、開始日時とタイトルの比較結果がともに同じ場合の順序も未決。

## 6. 同期・障害時の設計対象

正常系の処理順序案は「設定検証 → 取得 → 解析 → データ検証・参照解決 → 公開 → 結果記録」とする。

| 場面 | 未決の動作 |
| --- | --- |
| 起動 | 起動直後にも同期する。初回成功前に何を公開するかは未決 |
| 同期要求 | 手動と指定秒数間隔の自動同期は確定。自動同期の有効・無効と秒数はbundleConfigで設定。省略時の扱い、処理中の追加要求の扱いは未決 |
| 同期と編集の競合 | 同期でシートの内容に上書きすることは確定。取得中の編集を許可するか、操作を止めるかは未決 |
| 選択中のスケジュール | 再同期・開始時刻変更による順序の変化でどう追跡するか。選択対象がシートまたはDashboardから削除された場合にどうするか |
| 取得失敗 | 再試行の有無・条件・上限、利用側への通知方法 |
| 一部のシートの取得失敗 | 全体を失敗にするか、取得済みの範囲を公開するか |
| 行の不備・参照解決失敗 | 同期全体を失敗にするか、不正行のみ除外するか、警告付きで公開するか |
| 全件空・行の消失 | 有効な更新として反映するか、異常として扱うか |
| 再同期の失敗 | 前回成功データを保持するか、無効化するか |
| NodeCG再起動 | 前回成功データを復元するか、取得成功まで未準備とするか |

運用向けの診断情報として、シート種別、行番号、項目名、原因を提供する案とする。表示先と粒度はQ8で確認する。

## 7. 確認事項

### 7.1 基本構成の確認状況

| ID | 確認状況・残っている質問 | 影響する設計 |
| --- | --- | --- |
| Q1 | **回答反映済み**: シートはマスターかつ読み取り専用。Dashboard編集・同期・スケジュール選択が必要。Graphicsの要否はQ8に集約 | 機能範囲、更新方向、画面 |
| Q2 | **一部回答済み**: サービスアカウントを使用。資格情報の配置・供給方法に制約はあるか | Google接続、設定、運用手順 |
| Q3 | **回答反映済み**: ボタン同期と秒数指定の自動同期に対応。有効・無効と秒数はbundleConfigで設定。起動直後にも同期する。設定値・省略時・処理中の要求の扱いなどは第4.4節・第6章の残件 | 同期制御 |
| Q4 | **回答反映済み**: `name`で照合し、複数行が一致する場合は`game_name`・`category`も一致する行に絞り込む。絞り込み後の0件・複数件の扱いなどは第5.3節の残件 | 人物モデル、参照解決 |

### 7.2 データ契約・運用の確定に必要な事項

| ID | 質問 | 影響する設計 |
| --- | --- | --- |
| Q5 | **一部回答済み**: 同一スプレッドシート内の3つのタブを使用。見出し行・データ開始行、列順固定か見出し名で対応付けるか、追加列や空行の扱いは未決 | 接続先設定、取得範囲、解析 |
| Q6 | 第5章の入力形式・必須性・空白などの照合規則はどうするか。選択の追跡用にスケジュールIDを追加できるか。`start_time`の詳細はQ12 | 型、Schema、正規化・検証 |
| Q7 | 第6章の失敗・空データ・再起動時の動作はどうするか。最後に成功したデータの保持は必要か | 公開単位、再試行、永続化 |
| Q8 | 利用する他Bundleはあるか。必要な公開形式と同期状態・エラーの表示先は何か。Graphicsは必要か。Bundleの正式名称は何か | Replicant・メッセージ契約、画面、命名 |
| Q9 | **回答反映済み**: Dashboardの編集内容は、次回の手動・自動同期でシートの内容に上書きする。取得中の編集操作の扱いは第6章の残件 | マスターデータと運用データ、競合解決 |
| Q10 | **回答反映済み**: 3種類すべての値の変更・行の追加・削除が対象。手動の並べ替えは不要で、`start_time`順とする。編集の保存操作と参照中の人物を変更・削除する際の扱いは残件 | 編集画面、検証、操作契約 |
| Q11 | スケジュールの選択は単一か複数か。初期選択・解除・再起動時の保存はどうするか。再同期で行が移動・消失した際に選択をどう扱うか | 選択状態、スケジュールの識別 |
| Q12 | **回答反映済み**: シートの日付・時刻をイベントの現地日時として扱い、bundleConfigのタイムゾーンで解釈する。シートへのタイムゾーン記入は不要。空欄は末尾、同一開始日時はタイトル表記そのものの日本語の文字列比較順とし、読みは別管理しない。具体的な日時表記・セル形式、内部・公開形式などは第5.4節の残件 | 時刻の解析・検証、スケジュールの順序 |

基本動作の回答は反映済み。引き続き、シート構成、データ形式、障害時の動作、公開形式と選択の追跡などの残件を確認する。未回答のまま実装時の暗黙の判断へ委ねない。

## 8. 設計確定後の成果物と検証項目

回答後、以下を本書に反映して実装可能な設計へ更新する。

1. 機能範囲と対象外の機能。
2. bundleConfigの設定キー、型、必須性、設定例。シート名の設定は確定要件として含める。
3. 各シートの入力規約と、公開データの型・JSON Schema。
4. 人物とスケジュールの識別・参照解決規則。
5. 同期の起動条件、競合、失敗、公開、再起動時の動作。
6. Replicant名と構造、必要なら操作メッセージと応答形式。
7. 必要とされた画面の操作・表示仕様と実装ファイルの割り当て。

実装後の検証では、確定した仕様に応じて以下の期待結果を定義する。現時点ではテストケースの観点であり、未決の動作を期待値として固定しない。

- 1つのスプレッドシート内の3つのタブ、カンマ区切りの複数人、独自のタブ名。
- 同名人物、同一人物の複数スケジュール、参照先なし・複数候補。
- 同名人物をゲームタイトル・カテゴリで絞り込める場合と、絞り込み後も0件・複数件の場合。
- 日付をまたぐ開始日時、空欄の末尾配置、同一開始日時でのゲームタイトルの50音順。
- タイムゾーン記載のないシート日時をbundleConfigのイベントのタイムゾーンで解釈すること。実行サーバーや閲覧者の端末のタイムゾーンによって解釈が変わらないこと。
- bundleConfigによる自動同期の有効・無効と秒数指定、起動直後の同期。
- 空セル・空行・列不足・追加列・不正な時間や年の形式。
- 認証失敗、通信失敗、一部取得失敗、処理中の再同期要求。
- 初回取得前、再同期失敗、全件空、NodeCG再起動。
- Dashboard編集後・編集中の同期、秒数指定の自動同期、スケジュール選択と再同期の組み合わせ。
- 3種類の行追加・削除、参照されている人物の変更、開始時刻の編集に伴う順序変更と選択の追跡。
- 公開データと生成型・Schemaの整合、機密情報を公開データに含めないこと。

本ドラフト作成時点では実装の追加・変更をしていないため、ビルド・動作テストは対象外とする。
