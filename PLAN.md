# PLAN

## Concept
スピードランイベントを前提とした、Google Spreadsheet連携用NodeCG Bundleの開発。

## 運用・機能要件

- シートをマスターデータとして運用する。
- Google Spreadsheetへのアクセスは読み取り専用とし、シートへの書き戻しは行わない。
- Googleへの認証にはサービスアカウントを使用する。
- Dashboard上でデータを編集できること。
- 編集対象はプレイヤー一覧・解説者一覧・スケジュールのすべてとし、値の変更・行の追加・削除を可能にする。
- Dashboardで編集したデータは、手動・自動同期時にシートの内容で上書きする。
- Dashboardのボタン操作で明示的に同期できること。
- 指定秒数間隔での自動同期も可能であること。
- 自動同期の有効・無効と同期間隔の秒数はbundleConfigで設定する。
- 起動直後にも同期する。
- Dashboard上でスケジュールを選択できること。
- スケジュールは開始時刻（`start_time`）順とし、Dashboardでの手動の並べ替えは不要。
- 呼称は「スケジュール」に統一する。

### 開始時刻と並び順

- `start_time`は日付を含む日時とし、シートに記載された日時を記載者・イベントのタイムゾーンの現地時刻として扱う。
- イベントのタイムゾーンはbundleConfigで設定し、シートの日時を解釈する際に使用する。
- シートの各日時にタイムゾーンやUTCオフセットを記入する運用は要求しない。
- スケジュールは開始日時の早い順に並べる。
- `start_time`が空欄のスケジュールは末尾に配置する。
- 同一開始日時は通常想定しないが、存在する場合はゲームタイトル（`game_name`）の50音順で並べる。
- ゲームタイトルの読みは別カラムで管理せず、タイトル表記そのものを日本語の文字列比較で並べる。
- シートの具体的な日時表記・セル形式、空欄が複数ある場合や日時・タイトルともに同じ場合の順序は確認が必要。

### 人物の照合

- スケジュールの`players`はプレイヤー一覧の`name`と照合する。
- スケジュールの`commentators`は解説者一覧の`name`と照合する。
- プレイヤー一覧・解説者一覧にも、ゲームタイトル（`game_name`）とカテゴリ（`category`）のカラムがある前提とする。
- `name`に複数行が一致する場合は、スケジュールの`game_name`・`category`も一致する行に絞り込む。
- `name`・`game_name`・`category`の3項目で絞り込んでも複数行が一致する場合、または一致する行がない場合の扱いは確認が必要。

### 確認が必要な事項

- 人物の3項目での絞り込み後に一致が0件・複数件の場合の扱い。
- シートの具体的な日時表記・セル形式と、内部・公開データの日時形式。RFC 3339を内部・公開形式に採用するかは未決とし、シート入力にオフセットを要求する根拠にはしない。
- ゲームタイトルの文字列比較の詳細、並び順が確定しない場合の扱い。
- 自動同期の設定値・省略時の扱い、同期処理が重なった場合の扱い。
- データ形式、同期失敗時の扱い、選択中のスケジュールを再同期後にどう維持するか。

## シート構成

### 想定するシート
連携先は1つのGoogleスプレッドシートとし、以下はそのファイル内の各タブを指す。
実際のシート名はbundleConfigで定義する前提。
- プレイヤー一覧
- 解説者一覧
- スケジュール

### プレイヤー一覧シート
- `name`
- `twitch_id`
- `twitter_id`
- `youtube_id`
- `game_name`
- `category`

### 解説者一覧
- `name`
- `twitch_id`
- `twitter_id`
- `youtube_id`
- `game_name`
- `category`

### スケジュール
- `start_time` (イベントのタイムゾーンにおける開始日時。日付を含み、シートへのタイムゾーン記入は不要。早い順、空欄は末尾。同一開始日時はゲームタイトルの50音順)
- `game_name`
- `release_year`
- `category`
- `estimate`
- `players` (カンマ区切りで複数人定義)
- `commentators`(カンマ区切りで複数人定義)
